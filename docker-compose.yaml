version: "3.7"
services:

  postgres-db:
    image: postgres:11.5-alpine
    container_name: cards-postgres
    environment:
      - POSTGRES_USER=cards
      - POSTGRES_PASSWORD=cards
      - POSTGRES_DB=cards-backend
    ports:
      - "5432:5432"
